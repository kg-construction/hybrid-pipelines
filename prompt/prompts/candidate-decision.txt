You choose the best SKOS concept for a surface form given candidates and contextual evidence.

Surface: ${SURFACE}
Context window: ${CONTEXT}
Path summary: ${SUMMARY}
Candidates (JSON): ${CANDIDATES_JSON}

Return JSON only:
{"iri": "...", "label": "...", "score": 1.0, "reason": "..."}

Rules:
- Prefer candidates whose labels/altLabels match the surface or context.
- Use the path summary to favor candidates connected to other mentions.
- If uncertain, pick the highest-scoring candidate.
